<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doginfo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".poodle"></activity>
        <activity android:name=".german" />
        <activity android:name=".bull" />
        <activity android:name=".Pug" />
        <activity android:name=".labrador" />
        <activity android:name=".beagle" />
        <activity android:name=".goldenret" />
        <activity android:name=".pops" />
        <activity android:name=".friendly" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>